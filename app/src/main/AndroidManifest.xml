<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/launch"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aurora"
        tools:targetApi="31">
        <activity
            android:name=".ui.login.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Aurora.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".ui.login.DataUI"
            android:exported="true"
            android:label="@string/data_act_title"
            android:windowSoftInputMode="adjustResize"></activity>


        <activity
            android:name=".ui.login.analyze"
            android:configChanges="orientation|keyboardHidden"
            android:exported="true"
            android:label="@string/test_results"
            android:screenOrientation="landscape"></activity>


        <activity
            android:name=".ui.login.LoginActivity"
            android:configChanges="keyboardHidden"
            android:exported="true"
            android:label="@string/login_act_title"
            android:windowSoftInputMode="adjustResize"></activity>

        <activity
            android:name=".ui.login.DocsActivity"
            android:configChanges="orientation|keyboardHidden"
            android:exported="true"
            android:label="@string/docs_act_title"
            android:screenOrientation="fullSensor" />

    </application>

</manifest>